server:
  port: 8082

spring:
  config:
    activate:
      on-profile: default
  application:
    name: garments
  datasource:
    url: jdbc:mysql://${garments_db:localhost}:3306/garments_db
    username: root
    password: 123123
  jpa:
    hibernate:
      ddl-auto: update

app:
  environment:
    garments-host: ${garments-host:localhost}
    vuelos-host: ${vuelos-host:localhost}
    hoteles-host: ${hoteles-host:localhost}

--- # Testing configuration

server:
  port: ${random.int(5000,5100)}

spring:
  config:
    activate:
      on-profile: test
  application:
    name: garments
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:testdb
    username: root
    password: 123123
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true

app:
  environment:
    garments-host: none